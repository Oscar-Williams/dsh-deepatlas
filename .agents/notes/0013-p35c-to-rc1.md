# 0013 · ④⑤⑥ → P4.1 → RC1(2026-08-23)

## 交付清单(评审第六轮 ④-⑥ + P4 + RC)
④ AuditReport v1(audit-v1.ts):分节报告(provenance/package/sourceSignals/
  dependencyAudit/risk);源码信号=抓 main 入口+cordis.patch.yml 正则扫描
  (child-process/dynamic-eval/network/fs-write/native/credential,6 模式);
  措辞红线落实("risk signals,非安全判定");红色清单规则优先于 elevated。
⑤ 内容寻址审计缓存(audit-cache.ts):key=sha256(repo#commit|audit-v1),
  commit 变更/规则升版自动失效;audit 工具同 commit 直接复用。
⑥ P3.8:黄金集 30 条(10 类×3,7 条 popularity-overfit 对抗样本,
  mustNot 系统含官方 harness);基准脚本冻结 baseline——
  **Top1=13.3%,Top3=30%,mustNot@3=1,overfit 0/7,gate FAIL(如实)**。
  教训:黄金集 id 大小写必须与索引一致(首轮 3 处笔误致假阴性)。
P4 Entry Gate:代码化于 benchmark.mjs;当前 FAIL → P4.1 以"安静顾问"
  形态实现但不主动打扰(默认 silent)。
P4.1 advise 工具:任务→已装清单(dump-config)→缺口→建议 1-3 条;
  已有能力则 silent;无匹配亦 silent。
RC1:secret-scan.sh 三重扫描 PASS(工作区/全历史/已知指纹——
  扫描器曾自匹配自身常量与 gitignored deploy key,已修);
  rc1-checklist.md 如实记录(含 gate FAIL)。

## 测试 63→71(audit-v1/cache/advise)
## 决策:tag v0.1.0-rc.1 冻结功能;发布材料与收录申请推迟至基准达标。
