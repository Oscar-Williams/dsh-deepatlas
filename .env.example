# DeepAtlas 环境变量示例（复制为 .env 使用；.env 已由 .gitignore 排除）

# GitHub Token（可选）：提升 Search API 配额；建议使用仅访问公共仓库的低权限 token
DEEPATLAS_GITHUB_TOKEN=

# 覆盖数据目录（默认 ~/.dsh/deepatlas）
DEEPATLAS_DATA_DIR=

# 索引 TTL（小时，默认 24）
DEEPATLAS_TTL_HOURS=24
