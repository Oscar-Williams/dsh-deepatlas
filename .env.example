# DeepAtlas 环境变量示例(复制为 .env 使用;.env 已被 gitignore)

# GitHub Token(可选):提升 API 限额 10→30 次/分钟,只读 public_repo 即可
DEEPATLAS_GITHUB_TOKEN=

# 覆盖数据目录(默认 ~/.dsh/deepatlas)
DEEPATLAS_DATA_DIR=

# 索引 TTL(小时,默认 24)
DEEPATLAS_TTL_HOURS=24
