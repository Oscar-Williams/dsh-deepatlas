# v0.2.1 发布完整性检查表

目标：修复 `v0.2.0` 的“公开 tag 早于最终验证状态”问题。`v0.2.0` 保持不可变；所有后续修复进入新的 `v0.2.1` release commit 和 tag。

## 1. 版本范围

- [ ] 包含 `50f9491`：advise 测试字符串与 `capsEv` seed 修复。
- [ ] 包含 `52e8506`：nightly workflow 的 YAML `on` 键修复。
- [ ] 包含 `6041b71`：Find/Advise 共用受约束 capability schema。
- [ ] 包含 README/CHANGELOG 事实完整性提交。
- [ ] 不包含 HostIntentGate、Evidence v2 或 reranker 调整；这些功能单独评审和发布。

## 2. Release commit

- [ ] 将 `package.json` 和 `package-lock.json` 版本同步更新为 `0.2.1`。
- [ ] 将 README 安装命令从 `#v0.2.0` 更新为 `#v0.2.1`。
- [ ] 将 CHANGELOG 的 Unreleased 标题冻结为带日期的 `v0.2.1`。
- [ ] `npm ci` 成功，锁文件无漂移。
- [ ] `npm test`、`npm run typecheck`、`npm run typecheck:tests` 全部通过。
- [ ] `npm run build` 后，提交的 `lib/` 与 `src/` 一致。
- [ ] `git diff --check` 和变更范围内敏感信息扫描通过。
- [ ] release commit 合入 `main`，并记录唯一 commit SHA。

## 3. GitHub 验证

- [ ] `main` 对 release commit 的 CI 全绿。
- [ ] 从该 commit 创建新的 annotated tag `v0.2.1`，不移动任何既有 tag。
- [ ] 对 tag 安装路径执行 GitHub-by-tag 分发 E2E。
- [ ] 核对 tag 中的 `package.json`、README、CHANGELOG、workflow 和测试代码均为最终版本。
- [ ] 仅在 tag 验证通过后创建 GitHub Release，并确认 Release 指向同一 SHA。

## 4. 外部同步

- [ ] `awesome-dsh-plugin/awesome-dsh-plugin#2849` 保持单一提交范围，不混入 DeepAtlas 源码变更。
- [ ] `v0.2.1` 发布后再更新 PR 正文中的版本、测试数与评测限定语。
- [ ] 若维护者要求 rebase，只同步 fork 的 PR 分支；不改写 DeepAtlas 的公开 tag 或发布历史。
