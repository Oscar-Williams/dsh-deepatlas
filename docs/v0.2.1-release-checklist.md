# v0.2.1 发布完整性检查表

目标：修复 `v0.2.0` 的“公开 tag 早于最终验证状态”问题。`v0.2.0` 保持不可变；所有后续修复进入新的 `v0.2.1` release commit 和 tag。

## 1. 版本范围

- [x] 包含 `50f9491`：advise 测试字符串与 `capsEv` seed 修复。
- [x] 包含 `52e8506`：nightly workflow 的 YAML `on` 键修复。
- [x] 包含 `6041b71`：Find/Advise 共用受约束 capability schema。
- [x] 包含 DSH rc.2 lossless JSON、取消传播与 launcher 修复。
- [x] 包含完整 GitHub topic 分片扫描、主源降级保护与原子缓存写入。
- [x] 包含 audit-v3 fail-closed、缓存授权收口、安装快照/验证/恢复链路。
- [x] 包含中英文 README、架构、安全模型与 CHANGELOG 事实同步。
- [x] HostIntentGate、Evidence v2 与 reranker 保持独立版本线，采用完整 Gate 设计后发布。

## 2. Release commit

- [x] 将 `package.json` 和 `package-lock.json` 版本同步更新为 `0.2.1`。
- [x] 将 README 安装命令更新为 `#v0.2.1`。
- [x] 将 CHANGELOG 标题冻结为带日期的 `v0.2.1`。
- [x] `npm ci` 成功，锁文件无漂移。
- [x] `npm test`、`npm run typecheck`、`npm run typecheck:tests` 全部通过（22 files / 108 tests）。
- [x] `npm run build` 后，提交的 `lib/` 与 `src/` 一致。
- [x] `git diff --check` 和当前工作树敏感信息扫描通过。
- [ ] 全历史扫描仍报告早期已知凭据指纹；按本轮约束暂缓轮换与历史改写，发布后单独处理。
- [ ] release commit 合入 `main`，并记录唯一 commit SHA。

## 3. GitHub 验证

- [ ] `main` 对 release commit 的 CI 全绿。
- [ ] 从该 commit 创建新的 annotated tag `v0.2.1`，不移动任何既有 tag。
- [ ] 对 tag 安装路径执行 GitHub-by-tag 分发 E2E。
- [ ] 核对 tag 中的 `package.json`、README、CHANGELOG、workflow 和测试代码均为最终版本。
- [ ] 仅在 tag 验证通过后创建 GitHub Release，并确认 Release 指向同一 SHA。

## 4. 外部同步

- [ ] `awesome-dsh-plugin/awesome-dsh-plugin#2849` 保持单一提交范围，不混入 DeepAtlas 源码变更。
- [ ] `v0.2.1` 发布后再更新 PR 正文中的版本、测试数与评测限定语。
- [ ] 若维护者要求 rebase，只同步 fork 的 PR 分支；不改写 DeepAtlas 的公开 tag 或发布历史。
