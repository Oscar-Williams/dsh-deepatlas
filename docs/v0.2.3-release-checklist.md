# DeepAtlas v0.2.3 发布检查表

## Evidence v2

- [x] 公开 `index.schema.json` 与运行时 schema v2 同步，并由冻结原生 fixture 验证。
- [x] v1 索引确定性迁移为 `legacy-partial`，迁移结果无法通过发布 Gate。
- [x] GitHub Search、社区清单与 publisher artifact 分别标记 platform、community 与 publisher authority。
- [x] publisher manifest、README 和声明入口在同一完整 commit 下读取，并记录路径与内容 SHA-256。
- [x] 审计器与索引器共享固定 commit 的 artifact 读取和 manifest 入口解析。
- [x] accepted claim 需要 commit-pinned publisher evidence；平台与社区组合保持 provisional。
- [x] 来源失败、截断、超出容差的条目缺口、stale claims、无效 supersede 和 publisher provenance 缺失会阻断发布。

## 冻结 Gate

- [x] Evidence fixture release Gate：PASS。
- [x] EvidenceGold v1：12 cases；accepted precision 100%；recall 100%；must-not 假接受 0。
- [x] WSL2 Ubuntu 26.04 真实全量扫描：12,076 个去重插件；两源同轮成功；20,194 个 evidence atoms。
- [x] 全量索引：legacy 0；malformed 0；无效 publisher atom 0；accepted without pinned publisher 0。
- [x] 全量索引 structural Gate 与 release Gate：PASS。
- [x] 发布凭据已写入 [`benchmark/evidence-full-scan-receipt.json`](../benchmark/evidence-full-scan-receipt.json)。

## 工程与分发

- [x] Node 22/24、Ubuntu/Windows 测试矩阵由 GitHub CI 执行。
- [x] `lib/` 与 TypeScript 源码同步构建。
- [x] tarball 安装、`dump-config` 组合与启动冒烟由 distribution E2E 执行。
- [x] README 中英文、架构、路线图、CHANGELOG 与包描述使用统一产品定位和安装边界。
- [x] PR #5 最新提交的最终 CI 全部通过。
- [x] PR #5 完成 review，并以 merge commit `6421a96` 合入 `main`。
- [x] release commit 统一冻结 `v0.2.3` package/lockfile、README 安装入口、CHANGELOG 与发布说明；公开 `v0.2.2` tag 保持不变。

## 后续里程碑边界

Capability Diagnosis、HostIntentGate 和任务觉察进入 v0.2.4。v0.2.3 保持 Evidence v2、固定发布者证据与发布 Gate 的单一里程碑范围。
