# v0.2.2 发布检查表

目标：为锁定版本安装提供稳定的 HTTPS 路径，并保持源码、构建产物、文档、标签和 GitHub Release 一致。

## 版本内容

- [x] `package.json` 与 `package-lock.json` 更新为 `0.2.2`。
- [x] 中英文 README 的安装与更新命令指向 GitHub Codeload HTTPS 标签地址。
- [x] CHANGELOG、兼容契约与路线图统一到 `v0.2.2`。
- [x] `v0.2.1` 公开标签保持原位。
- [x] WSL 开发工具统一进入 `dsh-deepatlas` Conda 环境，独立 Node 目录完成清理。
- [x] 中英文 README 使用同一组全量扫描规模、耗时条件与提速指引。

## 验证

- [x] `npm ci`、类型检查、108 项测试与构建通过。
- [x] GitHub Codeload 标签安装在全新 WSL DSH_HOME 中通过。
- [x] `dump-config` 可见 `dsh-deepatlas`，Web profile 返回 HTTP 200。
- [ ] PR 合入 `main` 后五项 CI 全部通过。
- [ ] 从合并提交创建 annotated `v0.2.2` 标签。
- [ ] 从 `v0.2.2` 标签再次执行安装与启动验证。
- [ ] GitHub Release 指向同一合并提交。

## 后续版本线

- Evidence v2 由 `v0.2.3` 发布列车承接，HostIntentGate 由后续 `v0.2.x` 发布列车承接；两项工作使用冻结数据集、可重放记录和独立 Gate 完成验收。
- DSH 新 RC 继续经过 Windows/Linux canary、配置组合、工具调用与启动验证后进入兼容矩阵。
